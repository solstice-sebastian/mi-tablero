{{!-- symbol --}}{{!-- locked/projected PL --}}
{{!-- status --}} {{!-- type --}} {{!-- side --}}
{{!-- stopPrice --}} {{!-- slider for PL --}} {{!-- % for PL --}}
{{!-- price --}} {{!-- slider for stop price --}} {{!-- % for stop price --}}
{{!-- qty --}} {{!-- slider for qty --}} {{!-- % for base value --}}
{{!-- base toggle? --}}

<h2>{{order.symbol}} :
  <span class="asset-stat__value">
    {{pretty-percent value=profitLoss isColored=true}}
  </span>
</h2>

{{#unless order.isNew}}
  <span class="order-stat" data-test="order-status">{{order.status}}</span>
  <span class="order-stat" data-test="order-type">{{order.type}}</span>
  <span class="order-stat" data-test="order-side">{{order.side}}</span>
{{/unless}}

{{paper-input
  label = "Limit price"
  value = order.price
  onChange = "changeLimitPrice"
}}

{{paper-input
  label = stopPriceLabel
  value = order.stopPrice
  onChange = "changeStopPrice"
}}

{{paper-slider
  min = priceMin
  max = priceMax
  step = '0.00000001'
  value = order.price
  onChange = "changeLimitPrice"
  data-id = "limit-price-slider"
}}

{{paper-slider
  min = priceMin
  max = priceMax
  step = '0.00000001'
  value = order.stopPrice
  onChange = "changeStopPrice"
  data-id = "stop-price-slider"
}}